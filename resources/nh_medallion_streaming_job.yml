resources:
  jobs:
    nh_medallion_pipeline:
      name: "nh-medallion-streaming-pipeline"
      tasks:
        - task_key: run_medallion_streaming
          notebook_task:
            notebook_path: ../notebooks/run_medallion_pipeline.py
          existing_cluster_id: "${var.cluster_id}"
      max_concurrent_runs: 1
      continuous:
        pause_status: UNPAUSED
